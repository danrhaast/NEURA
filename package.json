{
  "name": "neura",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "next dev -p 9000",
    "build": "next build",
    "start": "next start -p 9000",
    "lint": "eslint .",
    "senha": "node scripts/gerar-senha.mjs",
    "usuario": "node --env-file-if-exists=.env --env-file-if-exists=.env.local scripts/usuario.mjs",
    "migrar": "node --env-file-if-exists=.env --env-file-if-exists=.env.local scripts/migrar.mjs"
  },
  "dependencies": {
    "@libsql/client": "^0.18.0",
    "next": "^15.5.4",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3.3.6",
    "eslint": "^9.39.5",
    "eslint-config-next": "^15.5.23"
  }
}
